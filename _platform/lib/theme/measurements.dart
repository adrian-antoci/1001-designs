import 'package:flutter/material.dart';

class AppMeasurements {
  static const double gapSm = 8.0;
  static const double gapMd = 16.0;
  static const double gapLg = 24.0;
  static const double gapXL = 32.0;
  static const double gapXXL = 48.0;

  static const EdgeInsets paddingSm = EdgeInsets.all(gapSm);
  static const EdgeInsets paddingMd = EdgeInsets.all(gapMd);
  static const EdgeInsets paddingLg = EdgeInsets.all(gapLg);

  static const EdgeInsets hPaddingSm = EdgeInsets.symmetric(horizontal: gapSm);
  static const EdgeInsets hPaddingMd = EdgeInsets.symmetric(horizontal: gapMd);
  static const EdgeInsets hPaddingLg = EdgeInsets.symmetric(horizontal: gapLg);

  static const EdgeInsets vPaddingSm = EdgeInsets.symmetric(vertical: gapSm);
  static const EdgeInsets vPaddingMd = EdgeInsets.symmetric(vertical: gapMd);
  static const EdgeInsets vPaddingLg = EdgeInsets.symmetric(vertical: gapLg);
}
